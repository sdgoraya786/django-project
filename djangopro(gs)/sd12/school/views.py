from django.shortcuts import render
from .models import Student
# Create your views here.

def home(request):
    # data  = Student.objects.all() # Get Data by Default Manager name
    # data  = Student.students.all() # Get Data by Custom Manager name
    pass
